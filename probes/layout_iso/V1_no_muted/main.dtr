//! GENERATED by scripts/gen_layout_iso.py - one layout per project.

pub struct V1_no_muted {
    id: Int
    kind: Str
    subsystem: Str
    priority: Int
    scope: Str
    scope_id: Int
    deferred_mode: Bool
    cancelled: Bool
    delivery_count: Int
}

pub fn s_new(id: Int, kind: Str, scope_id: Int) -> V1_no_muted {
    return V1_no_muted {
        id: id,
        kind: kind,
        subsystem: "",
        priority: 10,
        scope: "document",
        scope_id: scope_id,
        deferred_mode: false,
        cancelled: false,
        delivery_count: 0
    }
}

fn get_id(view x: V1_no_muted) -> Int => x.id
fn get_kind(view x: V1_no_muted) -> Str => x.kind
fn get_subsystem(view x: V1_no_muted) -> Str => x.subsystem
fn get_priority(view x: V1_no_muted) -> Int => x.priority
fn get_scope(view x: V1_no_muted) -> Str => x.scope
fn get_scope_id(view x: V1_no_muted) -> Int => x.scope_id
fn get_deferred_mode(view x: V1_no_muted) -> Bool => x.deferred_mode
fn get_cancelled(view x: V1_no_muted) -> Bool => x.cancelled
fn get_delivery_count(view x: V1_no_muted) -> Int => x.delivery_count

fn b2s(v: Bool) -> Str {
    mut res = "false"
    if v {
        res = "true"
    }
    return res
}

fn main() {
    mut xs: List<V1_no_muted> = []
    mut i = 0
    while i < 3 {
        xs = xs.push(s_new(i, "K", i))
        i = i + 1
    }
    i = 0
    while i < xs.len() {
        let x = xs[i]
        println("i=" + int_to_str(i) + " scope_id=" + int_to_str(get_scope_id(x)) + " deferred_mode=" + b2s(get_deferred_mode(x)) + " cancelled=" + b2s(get_cancelled(x)))
        i = i + 1
    }
}
