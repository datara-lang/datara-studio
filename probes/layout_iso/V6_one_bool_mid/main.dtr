//! GENERATED by scripts/gen_layout_iso.py - one layout per project.

pub struct V6_one_bool_mid {
    id: Int
    kind: Str
    deferred_mode: Bool
    scope_id: Int
}

pub fn s_new(id: Int, kind: Str, scope_id: Int) -> V6_one_bool_mid {
    return V6_one_bool_mid {
        id: id,
        kind: kind,
        deferred_mode: false,
        scope_id: scope_id
    }
}

fn get_id(view x: V6_one_bool_mid) -> Int => x.id
fn get_kind(view x: V6_one_bool_mid) -> Str => x.kind
fn get_deferred_mode(view x: V6_one_bool_mid) -> Bool => x.deferred_mode
fn get_scope_id(view x: V6_one_bool_mid) -> Int => x.scope_id

fn b2s(v: Bool) -> Str {
    mut res = "false"
    if v {
        res = "true"
    }
    return res
}

fn main() {
    mut xs: List<V6_one_bool_mid> = []
    mut i = 0
    while i < 3 {
        xs = xs.push(s_new(i, "K", i))
        i = i + 1
    }
    i = 0
    while i < xs.len() {
        let x = xs[i]
        println("i=" + int_to_str(i) + " deferred_mode=" + b2s(get_deferred_mode(x)) + " scope_id=" + int_to_str(get_scope_id(x)))
        i = i + 1
    }
}
