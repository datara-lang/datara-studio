//! GENERATED by scripts/gen_layout_iso.py - one layout per project.

pub struct V3_bools_then_int {
    id: Int
    kind: Str
    scope_id: Int
    deferred_mode: Bool
    cancelled: Bool
    delivery_count: Int
}

pub fn s_new(id: Int, kind: Str, scope_id: Int) -> V3_bools_then_int {
    return V3_bools_then_int {
        id: id,
        kind: kind,
        scope_id: scope_id,
        deferred_mode: false,
        cancelled: false,
        delivery_count: 0
    }
}

fn get_id(view x: V3_bools_then_int) -> Int => x.id
fn get_kind(view x: V3_bools_then_int) -> Str => x.kind
fn get_scope_id(view x: V3_bools_then_int) -> Int => x.scope_id
fn get_deferred_mode(view x: V3_bools_then_int) -> Bool => x.deferred_mode
fn get_cancelled(view x: V3_bools_then_int) -> Bool => x.cancelled
fn get_delivery_count(view x: V3_bools_then_int) -> Int => x.delivery_count

fn b2s(v: Bool) -> Str {
    mut res = "false"
    if v {
        res = "true"
    }
    return res
}

fn main() {
    mut xs: List<V3_bools_then_int> = []
    mut i = 0
    while i < 3 {
        xs = xs.push(s_new(i, "K", i))
        i = i + 1
    }
    i = 0
    while i < xs.len() {
        let x = xs[i]
        println("i=" + int_to_str(i) + " scope_id=" + int_to_str(get_scope_id(x)) + " deferred_mode=" + b2s(get_deferred_mode(x)) + " cancelled=" + b2s(get_cancelled(x)))
        i = i + 1
    }
}
